import * as classes from './ShowButton.module.css';

export default function ShowButton() {
  return (
    <button type="button" className={classes.button}>
      показать еще пять билетов!
    </button>
  );
}
